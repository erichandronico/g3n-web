const TaglineStrip = () => {
  return (
    <div className="relative overflow-hidden bg-navy py-9 text-center">
      <span className="font-display text-[clamp(1rem,2.4vw,1.4rem)] font-semibold tracking-[0.04em] text-white">
        Fluye sin límites. <span className="text-teal-soft">Optimiza todo.</span>
      </span>
    </div>
  )
}

export default TaglineStrip
